version: '3.8'

services:
  ltkk-wiki:
    container_name: ltkk-wiki
    build: .
    restart: unless-stopped
    ports:
      - "3009:3000"
    environment:
      - NODE_ENV=production
      - TZ=Asia/Jakarta
